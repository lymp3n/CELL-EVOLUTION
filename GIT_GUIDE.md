# Полное руководство по работе с Git

## 📋 Оглавление
1. [Базовый сценарий: Клонирование и первый коммит](#базовый-сценарий)
2. [Основные команды для ежедневного использования](#основные-команды)
3. [Работа в команде: Правила и workflows](#работа-в-команде)
4. [Решение частых проблем](#решение-проблем)
5. [Полезные сокращения и алиасы](#сокращения)

---

## 🚀 <a name="базовый-сценарий"></a>Базовый сценарий: Клонирование и первый коммит

### 1. Клонирование репозитория
```bash
# Клонирование по HTTPS (проще для новичков)
git clone https://github.com/lymp3n/CELL-EVOLUTION.git

# Переход в папку репозитория
cd .\CELL-EVOLUTION
```

### 2. Настройка пользователя (если не настроено глобально)
```bash
# Проверить текущие настройки
git config --list

# Установить имя и email (локально для этого репозитория)
git config user.name "Ваше Имя"
git config user.email "ваш.email@example.com"

# Или глобально (для всех репозиториев)
git config --global user.name "Ваше Имя"
git config --global user.email "ваш.email@example.com"
```

### 3. Создание и работа с веткой develop
```bash
# Переключиться на ветку develop (если она существует на удалённом репозитории)
git checkout develop

# Или создать новую ветку develop от main
git checkout main
git pull origin main              # Получить последние изменения
git checkout -b develop          # Создать и переключиться на develop
```

### 4. Внесение изменений, коммит и пуш
```bash
# 1. Внесите изменения в файлы (например, config.yaml)

# 2. Проверить статус изменений
git status

# 3. Добавить файлы для коммита
git add config.yaml              # Конкретный файл
# ИЛИ
git add .                        # Все изменённые файлы
# ИЛИ
git add -A                       # Все файлы, включая удалённые

# 4. Создать коммит с описанием
git commit -m "Обновление конфигурации: добавил настройки БД"

# 5. Отправить изменения на GitHub
git push -u origin develop       # Первый пуш в ветку develop
# В следующий раз можно просто:
git push
```

---

## 📝 <a name="основные-команды"></a>Основные команды для ежедневного использования

### Сценарий 1: Ежедневный workflow
```bash
# Утром: получить свежие изменения
git checkout develop
git pull origin develop

# Создать ветку для новой задачи
git checkout -b feature/new-feature

# Работа, изменения, тестирование...

# Добавить изменения и сделать коммит
git add .
git commit -m "Добавил новую функциональность X"

# Отправить ветку на GitHub
git push -u origin feature/new-feature

# Вечером: вернуться в develop и обновиться
git checkout develop
git pull origin develop
```

### Сценарий 2: Отмена изменений
```bash
# Отменить изменения в недобавленном файле
git checkout -- файл.txt

# Убрать файл из staged (перед коммитом)
git reset HEAD файл.txt

# Изменить последний коммит (если ещё не запушили)
git commit --amend -m "Новое описание коммита"

# Откатить коммит (создаст новый коммит с отменой)
git revert хэш_коммита

# Жёсткий откат (осторожно! удалит изменения)
git reset --hard хэш_коммита
```

### Сценарий 3: Работа с ветками
```bash
# Посмотреть все ветки
git branch -a                    # Все ветки (локальные и удалённые)
git branch                       # Только локальные

# Удалить ветку
git branch -d feature/old        # Локально
git push origin --delete feature/old  # На удалённом репозитории

# Переименовать ветку
git branch -m старое-имя новое-имя

# Слияние веток
git checkout develop
git merge feature/new-feature    # Влить feature в develop
```

### Сценарий 4: История и поиск
```bash
# Красивая история
git log --oneline --graph --all --decorate

# История с изменениями в файлах
git log -p

# Поиск по сообщениям коммитов
git log --grep="исправление"

# Кто последний менял строку в файле
git blame файл.txt
```

---

## 👥 <a name="работа-в-команде"></a>Работа в команде: Правила и workflows

### Основные правила
1. **Never push to main/develop directly** — используйте Pull Requests
2. **Один feature = одна ветка** — изолируйте изменения
3. **Частые коммиты** — маленькие, осмысленные изменения
4. **Осмысленные сообщения коммитов** — что сделано и зачем

### Стандартный Git Flow
```bash
main        # Продакшн (только релизные версии)
├── develop # Разработка (интеграционная ветка)
│   ├── feature/user-auth    # Ветка для аутентификации
│   ├── feature/payment      # Ветка для оплаты
│   └── hotfix/critical-bug  # Срочное исправление
```

### Процесс code review
```bash
# 1. Создать ветку от develop
git checkout develop
git pull
git checkout -b feature/my-feature

# 2. Разработка, коммиты
git add .
git commit -m "Описание изменений"
git push -u origin feature/my-feature

# 3. Создать Pull Request на GitHub
# Обсуждать, вносить правки...

# 4. После апрува - смержить
git checkout develop
git pull
git merge --no-ff feature/my-feature  # Сохраняет историю
git push

# 5. Удалить ветку
git branch -d feature/my-feature
git push origin --delete feature/my-feature
```

### Правила именования веток
```bash
feature/краткое-описание    # Новая функциональность
bugfix/описание-бага        # Исправление ошибки
hotfix/срочное-исправление  # Критичный баг в production
release/1.2.0               # Подготовка релиза
docs/обновление-документации # Изменения в документации
```

### Конвенция коммитов
```
тип(область): описание изменений

feat(auth): добавил OAuth2 авторизацию
fix(api): исправил обработку 404 ошибок
docs(readme): обновил инструкцию установки
style(css): применил кодстайл к файлам
refactor(utils): переписал функцию хеширования
test(auth): добавил тесты для логина
chore(deps): обновил версию библиотеки X
```

---

## 🔧 <a name="решение-проблем"></a>Решение частых проблем

### Конфликты при слиянии
```bash
# 1. Перед слиянием обнови ветку
git checkout develop
git pull origin develop

# 2. Попытаться смержить
git merge feature/branch

# 3. Если конфликт:
#    - Git покажет конфликтующие файлы
#    - Отредактируй их, оставив нужный код
#    - Удали маркеры конфликта <<<<<<<, =======, >>>>>>>

# 4. После разрешения:
git add конфликтующий_файл.txt
git commit -m "Разрешил конфликт с feature/branch"
```

### "Your local changes would be overwritten"
```bash
# Сохранить изменения временно
git stash

# Выполнить нужную операцию (pull, checkout)
git pull origin develop

# Вернуть сохранённые изменения
git stash pop
```

### Неверный коммит в main/develop
```bash
# 1. Отменить последний коммит (без удаления изменений)
git reset --soft HEAD~1

# 2. Создать новую ветку для изменений
git checkout -b feature/correct-branch

# 3. Закоммитить и запушить
git commit -m "Правильное сообщение"
git push -u origin feature/correct-branch
```

### Удалил важную ветку локально
```bash
# Посмотреть историю всех веток (даже удалённых)
git reflog

# Восстановить ветку по хэшу из reflog
git checkout -b восстановленная-ветка хэш_коммита
```
